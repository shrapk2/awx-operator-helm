apiVersion: v1
kind: Service
metadata:
  name: {{ include "awx-operator.fullname" . }}
  labels:
    {{- include "awx-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "awx-operator.selectorLabels" . | nindent 4 }}
